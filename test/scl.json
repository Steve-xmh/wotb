{
    "exec_file": "./sharp-craft-launcher.exe",
    "exec_args": [],
    "bench_amount": 20,
    "window_script": "for _, v in ipairs(windows) do if v.name == 'Sharp Craft Launcher' then return true end end"
}